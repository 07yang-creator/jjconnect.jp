# Right Sidebar Component - 架构图

## 📐 项目结构

```
jjconnect.jp/
│
├── components/
│   └── layout/
│       ├── RightSidebar.tsx      # React 组件（Server Component）
│       └── RightSidebar.js       # JavaScript 模块（可独立使用）
│
├── app/
│   ├── layout.tsx                # Next.js 根布局
│   └── globals.css               # 全局样式
│
├── lib/
│   └── supabase/
│       ├── client.ts             # Supabase 客户端配置
│       └── server.ts             # 服务端工具函数
│
├── types/
│   └── database.ts               # TypeScript 类型定义
│
├── 示例和文档/
│   ├── sidebar-example.html      # 完整独立示例
│   ├── sidebar-snippet.html      # 快速集成片段
│   ├── sidebar-preview.html      # 交互式预览
│   ├── RIGHT_SIDEBAR_GUIDE.md    # 详细使用指南
│   ├── SIDEBAR_SETUP_CHECKLIST.md # 配置清单
│   └── RIGHT_SIDEBAR_SUMMARY.md   # 项目总结
│
└── .env.local                    # 环境变量（需创建）
    ├── NEXT_PUBLIC_SUPABASE_URL
    └── NEXT_PUBLIC_SUPABASE_ANON_KEY
```

## 🔄 数据流图

```
┌─────────────────────────────────────────────────────────────┐
│                      Right Sidebar Component                 │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐  │
│  │   Search     │    │  Categories  │    │   Featured   │  │
│  │     Box      │    │    Section   │    │   Content    │  │
│  └──────────────┘    └──────────────┘    └──────────────┘  │
│         │                    │                    │          │
│         │                    │                    │          │
│         ▼                    ▼                    ▼          │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Supabase Client (JS/TS)                 │  │
│  └──────────────────────────────────────────────────────┘  │
│                             │                                │
└─────────────────────────────┼────────────────────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │   Supabase       │
                    │   PostgreSQL     │
                    │   Database       │
                    └──────────────────┘
                              │
                    ┌─────────┴─────────┐
                    │                   │
                    ▼                   ▼
            ┌──────────────┐    ┌──────────────┐
            │  categories  │    │    posts     │
            │     表       │    │     表       │
            └──────────────┘    └──────────────┘
                    │
                    ▼
            ┌──────────────┐
            │   profiles   │
            │     表       │
            └──────────────┘
```

## 🎯 功能模块图

```
┌────────────────────────────────────────────────────────────┐
│                   Right Sidebar                             │
└────────────────────────────────────────────────────────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│   Search     │  │  Navigation  │  │  User State  │
│   Module     │  │   Module     │  │   Module     │
└──────────────┘  └──────────────┘  └──────────────┘
        │                 │                 │
        │         ┌───────┴───────┐         │
        │         │               │         │
        ▼         ▼               ▼         ▼
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│  Search  │ │Categories│ │ Featured │ │ Auth     │
│   Form   │ │   List   │ │  Posts   │ │ Check    │
└──────────┘ └──────────┘ └──────────┘ └──────────┘
                                              │
                                    ┌─────────┴─────────┐
                                    │                   │
                                    ▼                   ▼
                            ┌──────────────┐  ┌──────────────┐
                            │ Login Prompt │  │  Shortcuts   │
                            │ (未登录)      │  │  (已授权)     │
                            └──────────────┘  └──────────────┘
```

## 📱 响应式布局图

```
┌─────────────────────────────────────────────────────────────┐
│                    Desktop (≥1024px)                         │
├──────────────────────────────────────┬──────────────────────┤
│                                      │  Right Sidebar       │
│          Main Content                │  Width: 320px        │
│          (Full Width)                │                      │
│                                      │  ┌────────────────┐  │
│  Lorem ipsum dolor sit amet...       │  │ 🔍 Search      │  │
│  ┌──────────┐  ┌──────────┐         │  │ 📂 Categories  │  │
│  │  Card 1  │  │  Card 2  │         │  │ ⚡ Shortcuts   │  │
│  └──────────┘  └──────────┘         │  │ 💎 Featured    │  │
│                                      │  └────────────────┘  │
└──────────────────────────────────────┴──────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    Tablet (768-1023px)                       │
├────────────────────────────────┬────────────────────────────┤
│                                │  Right Sidebar             │
│     Main Content               │  Width: 256px              │
│     (Adjusted)                 │                            │
│                                │  ┌──────────────────────┐  │
│  Lorem ipsum dolor...          │  │ 🔍 Search            │  │
│  ┌────────┐                    │  │ • Categories         │  │
│  │ Card 1 │                    │  │ • Shortcuts          │  │
│  └────────┘                    │  │ [Featured - Simple]  │  │
│                                │  └──────────────────────┘  │
└────────────────────────────────┴────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    Mobile (<768px)                           │
├────────────────────────────────┬────────────────────────────┤
│                                │ Sidebar                    │
│  Main Content                  │ 80px                       │
│  (Narrow)                      │                            │
│                                │ ┌───┐                      │
│  Lorem ipsum...                │ │ 🔍 │                     │
│  ┌─────────┐                   │ │ • │                      │
│  │  Card   │                   │ │ • │                      │
│  └─────────┘                   │ │ • │                      │
│                                │ └───┘                      │
│                                │ (Icons Only)               │
└────────────────────────────────┴────────────────────────────┘
```

## 🔐 权限流程图

```
                    ┌──────────────┐
                    │  User Visits │
                    │     Page     │
                    └──────┬───────┘
                           │
                           ▼
                ┌──────────────────────┐
                │ Check Auth Status    │
                │ supabase.auth.getUser()│
                └──────┬───────────────┘
                       │
           ┌───────────┴───────────┐
           │                       │
           ▼                       ▼
    ┌──────────┐          ┌──────────────┐
    │ No User  │          │  User Found  │
    └────┬─────┘          └──────┬───────┘
         │                       │
         ▼                       ▼
    ┌──────────────┐     ┌─────────────────┐
    │ Show Login   │     │ Check Profile   │
    │   Prompt     │     │ is_authorized   │
    └──────────────┘     └────────┬────────┘
                                  │
                      ┌───────────┴────────────┐
                      │                        │
                      ▼                        ▼
            ┌──────────────────┐    ┌──────────────────┐
            │ is_authorized    │    │ is_authorized    │
            │   = false        │    │   = true         │
            └────────┬─────────┘    └────────┬─────────┘
                     │                       │
                     ▼                       ▼
            ┌──────────────┐       ┌──────────────────┐
            │ Show Basic   │       │ Show Basic +     │
            │ Content      │       │ Admin Shortcuts  │
            └──────────────┘       └──────────────────┘
```

## 🎨 CSS 类名结构

```
.right-sidebar
├── .search-section
│   ├── .search-form
│   └── .search-button
├── .categories-section
│   ├── .category-link (multiple)
│   │   ├── .category-dot
│   │   └── .category-name
├── .shortcuts-section (conditional)
│   └── .shortcut-link (multiple)
│       ├── .shortcut-icon
│       └── .shortcut-text
├── .featured-section
│   └── .featured-card (multiple)
│       ├── .featured-image
│       ├── .featured-title
│       └── .featured-meta
└── .login-section (conditional)
    ├── .login-card (desktop)
    └── .login-button (mobile)
```

## 🔧 JavaScript 函数架构

```
main.js
├── initRightSidebar()              # 主初始化函数
│   ├── loadCategories()            # 加载分类
│   │   ├── supabase.from('categories').select()
│   │   └── renderCategories()
│   ├── loadFeaturedPosts()         # 加载精选
│   │   ├── supabase.from('posts').select()
│   │   └── renderFeaturedPosts()
│   └── checkAuth()                 # 检查认证
│       ├── supabase.auth.getUser()
│       ├── supabase.from('profiles').select()
│       └── updateAuthUI()
└── Event Listeners
    └── onAuthStateChange()         # 监听认证变化
```

## 📊 数据库表关系

```
┌─────────────────┐
│   auth.users    │
│   (Supabase)    │
└────────┬────────┘
         │
         │ 1:1
         ▼
┌─────────────────┐        ┌─────────────────┐
│    profiles     │        │   categories    │
│  ─────────────  │        │  ─────────────  │
│  • id (PK, FK)  │        │  • id (PK)      │
│  • display_name │        │  • name         │
│  • is_authorized│        │  • slug         │
└────────┬────────┘        │  • description  │
         │                 └────────┬────────┘
         │                          │
         │ 1:N                      │ 1:N
         │                          │
         ▼                          │
┌─────────────────┐                 │
│      posts      │◄────────────────┘
│  ─────────────  │
│  • id (PK)      │
│  • title        │
│  • content      │
│  • author_id (FK)
│  • category_id (FK)
│  • is_paid      │
│  • price        │
│  • status       │
└─────────────────┘
         │
         │ 1:N
         ▼
┌─────────────────┐
│ user_categories │
│  ─────────────  │
│  • id (PK)      │
│  • user_id (FK) │
│  • name         │
└─────────────────┘
```

## 🌐 集成方式对比

```
┌────────────────────────────────────────────────────────────┐
│                   Integration Methods                       │
├──────────────────────┬─────────────────────────────────────┤
│   Next.js (SSR)      │   Standalone HTML                   │
├──────────────────────┼─────────────────────────────────────┤
│ ✅ Server Components │ ✅ No build required                │
│ ✅ Type Safety       │ ✅ Easy to integrate                │
│ ✅ SEO Friendly      │ ✅ Works with any backend           │
│ ❌ Requires build    │ ❌ No SSR                           │
│ ❌ More complex      │ ❌ Client-side rendering only       │
└──────────────────────┴─────────────────────────────────────┘
```

## 📦 依赖关系图

```
Right Sidebar Component
        │
        ├── Tailwind CSS (Styling)
        │   └── CDN / npm package
        │
        ├── Supabase JS (Data)
        │   ├── @supabase/supabase-js
        │   └── @supabase/ssr (Next.js only)
        │
        └── TypeScript (Optional)
            └── Type definitions
```

## 🚦 加载时序图

```
Page Load
    │
    ├─→ HTML Parsing
    │       │
    │       ├─→ Load Tailwind CSS
    │       └─→ Load Supabase JS
    │
    ├─→ DOM Ready
    │       │
    │       └─→ initRightSidebar()
    │               │
    │               ├─→ loadCategories() ────┐
    │               │                        │ (Parallel)
    │               ├─→ loadFeaturedPosts() ─┤
    │               │                        │
    │               └─→ checkAuth() ─────────┘
    │                           │
    │                           └─→ All data loaded
    │
    └─→ Render Complete
            │
            └─→ User Interaction Ready
```

## 🎯 性能优化策略

```
┌─────────────────────────────────────────────────────────┐
│              Performance Optimization                    │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  1. Initial Load                                         │
│     • Lazy load images (loading="lazy")                 │
│     • Defer non-critical JS                             │
│     • Use CDN for libraries                             │
│                                                          │
│  2. Data Fetching                                        │
│     • Limit query results (5-10 items)                  │
│     • Parallel API calls (Promise.all)                  │
│     • Cache responses (optional)                        │
│                                                          │
│  3. Rendering                                            │
│     • Virtual scrolling (if needed)                     │
│     • Debounce search input                             │
│     • Optimize re-renders                               │
│                                                          │
│  4. User Experience                                      │
│     • Loading indicators                                │
│     • Error boundaries                                  │
│     • Smooth transitions (CSS)                          │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

**图表创建日期**: 2026-02-15  
**版本**: 1.0.0
